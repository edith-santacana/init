#Q3. What file has to be modified to make it permanent?

#!/bin/bash
echo "We need to edit a file called /etc/hostname: >> vi /etc/hostname"
echo "Set new hostname: didi.com"
echo "Save and close the file"